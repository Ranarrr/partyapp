<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.partyspottr.appdir">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.registerui.RegisterActivity" />
        <activity android:name=".ui.registerui.Register2Activity" />
        <activity android:name=".ui.registerui.Register3Activity" />
        <activity android:name=".ui.registerui.Register4Activity" />
        <activity android:name=".ui.registerui.Register5Activity" />
        <activity android:name=".ui.ProfilActivity" />
        <activity android:name=".ui.other_ui.EventDetails" />
    </application>

</manifest>